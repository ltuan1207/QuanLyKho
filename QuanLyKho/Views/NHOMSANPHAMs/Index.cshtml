@model IEnumerable<QuanLyKho.Models.NHOMSANPHAM>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenNhomSP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mota)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenNhomSP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mota)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaNhomSP }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaNhomSP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaNhomSP })
        </td>
    </tr>
}

</table>
