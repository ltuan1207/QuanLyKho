@model IEnumerable<QuanLyKho.Models.KHOHANG>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenKhohang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DiaChi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SDTKho)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenKhohang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DiaChi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SDTKho)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaKhoHang }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaKhoHang }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaKhoHang })
        </td>
    </tr>
}

</table>
