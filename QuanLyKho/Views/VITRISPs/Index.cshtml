@model IEnumerable<QuanLyKho.Models.VITRISP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SANPHAM.TenSP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VITRIKHO.TenVTK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoLuong)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SANPHAM.TenSP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VITRIKHO.TenVTK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoLuong)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaVTSP }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaVTSP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaVTSP })
        </td>
    </tr>
}

</table>
